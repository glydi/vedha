# ──────────────────────────────────────────────────────────
#  Render Production Profile
# ──────────────────────────────────────────────────────────

spring.application.name=vedha

# ── Database (Render PostgreSQL — Singapore) ─────────────
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://npvwzgnwprxt.db.dbaas.dev:30731/PwKFWx}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:pGYRXxF}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:ZGGAzvC}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ── Server ───────────────────────────────────────────────
server.port=${PORT:8080}

# ── JWT ──────────────────────────────────────────────────
vedha.jwt.secret=${JWT_SECRET:this-is-a-very-secure-static-secret-key-for-vedha-01}

# ── CORS ─────────────────────────────────────────────────
vedha.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:*}
